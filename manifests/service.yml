apiVersion: v1
kind: Service
metadata:
    name: strapiagg
spec:
    type: ClusterIP
    ports:
        - protocol: TCP
          port: 80
          targetPort: 1337
    selector:
        app: strapiagg